<h2>Create a new User</h2>

<%= render "form.html", changeset: @changeset,
                        action: Router.user_path(@conn, :create) %>

<%= link "Back", to: Router.user_path(@conn, :index) %>
